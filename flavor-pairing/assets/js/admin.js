/* global FlavorPairingAdmin */
( function () {
	// Placeholder for admin side enhancements.
	if ( 'undefined' === typeof FlavorPairingAdmin ) {
		return;
	}
	console.debug( 'Flavor Pairing admin bootstrapped', FlavorPairingAdmin ); // eslint-disable-line no-console
}() );
